{{/* Define reusable template helpers here.
     Common patterns:
       - a `pedelec.labels` define block for common labels
       - a `pedelec.image` define block that builds a full image reference */}}
